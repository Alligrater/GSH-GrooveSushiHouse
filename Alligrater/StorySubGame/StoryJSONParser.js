

function loadinStoryJSON(jsonpath){
    var fs = require('fs');
    var content = fs.readFileSync(jsonpath);

    //Parsing
    var JSONContent = JSON.parse(content);
    //This needs to be modified to fit the current style


    //?
}

